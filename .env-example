# Default number of objects per page
PER_PAGE = 9

#python -c "import string as s;from random import SystemRandom 
#as sr;print(''.join(sr().choices(s.ascii_letters + s.punctuation, k=64)))"
SECRET_KEY="$Nt)-zjgE^HHKwFP)hrx.DJB*oc|N:|CeP&GSkNJiN;n+;<T.mFrWf$wrhSds"

# Django secret key
SECRET_KEY = 'CHANGE-ME'

# simple JWT SECRET_KEY
SECRET_KEY_JWT = "secret-key-jwt"

# 0 = False - 1 = True
DEBUG = 0

# 0 = False(disable), 1 = True(enable)
SELENIUM_HEADLESS=1

CORS_ALLOWED_ORIGINS = "http://127.0.0.1:5500,"

